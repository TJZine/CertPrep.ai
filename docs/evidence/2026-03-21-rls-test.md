# Supabase RLS test evidence (2026-03-21)

## Summary

Attempted to run the existing security test `tests/security/rls.test.ts`.

In this environment the test suite is **skipped** because the required Supabase credentials are not set.
No RLS verification was performed against a non-production Supabase instance in this workspace.

## Required environment variables

The test file gates execution on all of the following:

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `RUN_RLS_TESTS=true`

Optional safety configuration:

- `PRODUCTION_SUPABASE_HOSTS` (comma-separated hostnames that must never be cleared)

## Environment status (this run)

All required variables were `UNSET`:

- `NEXT_PUBLIC_SUPABASE_URL=UNSET`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY=UNSET`
- `SUPABASE_SERVICE_ROLE_KEY=UNSET`
- `RUN_RLS_TESTS=UNSET`
- `PRODUCTION_SUPABASE_HOSTS=UNSET`

## Command output

Command:

```bash
npm test -- tests/security/rls.test.ts
```

Result:

```text
Test Files  1 skipped (1)
Tests       3 skipped (3)
```

## Next steps (to produce real RLS evidence)

1. Configure a **non-production** Supabase instance and set the required env vars (prefer `.env.local`).
2. Set `PRODUCTION_SUPABASE_HOSTS` to include any production Supabase hostnames so the test clear step can never touch them.
3. Re-run `npm test -- tests/security/rls.test.ts` and update this file with the full passing output.
